 }
 
+const mcColors = {
+  '0': '#000000', '1': '#0000aa', '2': '#00aa00', '3': '#00aaaa',
+  '4': '#aa0000', '5': '#aa00aa', '6': '#ffaa00', '7': '#aaaaaa',
+  '8': '#555555', '9': '#5555ff', 'a': '#55ff55', 'b': '#55ffff',
+  'c': '#ff5555', 'd': '#ff55ff', 'e': '#ffff55', 'f': '#ffffff'
+}
+
+function minecraftText(value) {
+  const lines = Array.isArray(value) ? value : [value]
+  return lines.map(line => {
+    const text = String(line ?? '')
+    let out = ''
+    let buffer = ''
+    let color = null
+    let bold = false
+    let italic = false
+    let underline = false
+    const flush = () => {
+      if (!buffer) return
+      const styles = []
+      if (color) styles.push(`color:${color}`)
+      if (bold) styles.push('font-weight:700')
+      if (italic) styles.push('font-style:italic')
+      if (underline) styles.push('text-decoration:underline')
+      out += `<span${styles.length ? ` style="${styles.join(';')}"` : ''}>${escapeHtml(buffer)}</span>`
+      buffer = ''
+    }
+    for (let i = 0; i < text.length; i++) {
+      if (text[i] !== '§' || i + 1 >= text.length) {
+        buffer += text[i]
+        continue
+      }
+      flush()
+      const code = text[++i].toLowerCase()
+      if (mcColors[code]) {
+        color = mcColors[code]
+        bold = italic = underline = false
+      } else if (code === 'l') bold = true
+      else if (code === 'o') italic = true
+      else if (code === 'n') underline = true
+      else if (code === 'r') {
+        color = null
+        bold = italic = underline = false
+      }
+    }
+    flush()
+    return `<div>${out || '&nbsp;'}</div>`
+  }).join('')
+}
+
+function minecraftIdentity(mc) {
+  if (!mc?.online) return ''
+  const rawMotd = mc.motd?.raw || mc.motd?.clean || []
+  const icon = mc.icon ? `<img class="mcicon" src="${escapeHtml(mc.icon)}" width="64" height="64" alt="Server icon">` : '<div class="mcicon mciconempty"></div>'
+  return `<div class="mcidentity">${icon}<div class="mcidentitytext"><div class="mcmotd">${minecraftText(rawMotd)}</div><div class="mcsubline">${escapeHtml(mc.version || 'Minecraft')} · ${escapeHtml(mc.players ? `${mc.players.online}/${mc.players.max} players` : 'player count unavailable')}</div></div></div>`
+}
+
 function minecraftPanel(label, mc) {
   if (!mc) return block(label, '<div class="notice">No Minecraft status response.</div>')
   const online = mc.online === true
-  const motd = mc.motd?.clean || mc.motd?.raw || []
   const plugins = mc.plugins || []
   const mods = mc.mods || []
   const players = mc.players?.list || []
+  const protocol = mc.protocol?.name || mc.protocol?.version || mc.protocol
   const status = kv([
     ['Online', online, online ? 'good' : 'bad'],
     ['IP', mc.ip],
     ['Port', mc.port ? `${mc.port}${mc.portSource ? ` · ${mc.portSource}` : ''}` : null],
     ['Hostname', mc.hostname],
     ['Version', mc.version],
-    ['Protocol', mc.protocol ? JSON.stringify(mc.protocol) : null],
+    ['Protocol', protocol],
     ['Players', mc.players ? `${mc.players.online}/${mc.players.max}` : null],
-    ['MOTD', Array.isArray(motd) ? motd.join(' / ') : motd],
-    ['Map', mc.map ? JSON.stringify(mc.map) : null],
+    ['Map', mc.map?.name || mc.map],
     ['Software', mc.software],
-    ['EULA blocked', mc.eula_blocked],
-    ['Debug', mc.debug ? JSON.stringify(mc.debug) : null]
+    ['EULA blocked', mc.eula_blocked]
   ])
+  const details = mc.debug ? `<details class="statusdetails"><summary>Status details</summary><pre>${escapeHtml(JSON.stringify(mc.debug, null, 2))}</pre></details>` : ''
   const extra = `${plugins.length ? `<h3>Plugins</h3>${table(['name', 'version'], plugins.map(p => [p.name, p.version]))}` : ''}${mods.length ? `<h3>Mods</h3>${table(['name', 'version'], mods.map(p => [p.name, p.version]))}` : ''}${players.length ? `<h3>Visible players</h3>${table(['name', 'uuid'], players.map(p => [p.name, p.uuid]))}` : ''}`
-  const icon = mc.icon ? `<div class="notice"><img src="${escapeHtml(mc.icon)}" width="48" height="48" alt="Server icon" style="vertical-align:middle;margin-right:12px;image-rendering:pixelated"><span>Server icon returned by status ping</span></div>` : ''
-  return block(label, `${icon}${status}${extra}`)
+  return block(label, `${minecraftIdentity(mc)}${status}${details}${extra}`)
 }
 
 function renderMinecraft(data) {
@@ -686,6 +751,7 @@ async function performLookup(target) {
           scanMode: settings.scanMode,
           scanTimeout: settings.scanTimeout,
           minecraft: settings.minecraft,
+          locationCrossCheck: settings.locationCrossCheck,
           lookupMode
         }
       })
